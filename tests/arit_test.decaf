class Program {
  void main() {
    int a;
    int b;
    int c;
    boolean d;
    boolean e;
    a = c * b + c / b;
    b = a + b * c;
    b = c * (a + c) / a;
    b = (c * a) + (c / a);
    d = true || false && true;
    e = d && false || d;

    if(5 < 4 || 7 < 8 && 4 != 7){
      a = 0;
    }else{
        
    }
  }
}
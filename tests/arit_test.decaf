class Program {
  void main() {
    int a;
    int b;
    int c;
    int d;
    int e;
    a = c * d + c / d;
    b = c * (d + c) / d;
    b = (c * d) + (c / d);
    d = a || b && c;
    e = a && b || c;
  }
}